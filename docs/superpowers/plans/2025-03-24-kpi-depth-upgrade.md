# KPI Depth Upgrade Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Upgrade 12 KPI JSON files to a new high-depth standard with 5 sections, 3 detailed paragraphs per section, concrete math/tools, and a senior operating partner tone.

**Architecture:** Each JSON file in `content/curated-articles/` will be rewritten. The existing structure of `articleSections` (title, body, optional callout) will be maintained but expanded.

**Tech Stack:** JSON, Markdown-style content within JSON strings.

---

### Task 1: Update `branch-profitability.json`
**Files:**
- Modify: `content/curated-articles/branch-profitability.json`
- [ ] **Step 1:** Rewrite `branch-profitability.json` to the high-depth standard.
- [ ] **Step 2:** Validate JSON structure and section count.

### Task 2: Update `campaign-roi.json`
**Files:**
- Modify: `content/curated-articles/campaign-roi.json`
- [ ] **Step 1:** Rewrite `campaign-roi.json` to the high-depth standard.
- [ ] **Step 2:** Validate JSON structure and section count.

### Task 3: Update `defect-rate.json`
**Files:**
- Modify: `content/curated-articles/defect-rate.json`
- [ ] **Step 1:** Rewrite `defect-rate.json` to the high-depth standard.
- [ ] **Step 2:** Validate JSON structure and section count.

### Task 4: Update `employee-turnover.json`
**Files:**
- Modify: `content/curated-articles/employee-turnover.json`
- [ ] **Step 1:** Rewrite `employee-turnover.json` to the high-depth standard.
- [ ] **Step 2:** Validate JSON structure and section count.

### Task 5: Update `mrr.json`
**Files:**
- Modify: `content/curated-articles/mrr.json`
- [ ] **Step 1:** Rewrite `mrr.json` to the high-depth standard.
- [ ] **Step 2:** Validate JSON structure and section count.

### Task 6: Update `on-time-delivery.json`
**Files:**
- Modify: `content/curated-articles/on-time-delivery.json`
- [ ] **Step 1:** Rewrite `on-time-delivery.json` to the high-depth standard.
- [ ] **Step 2:** Validate JSON structure and section count.

### Task 7: Update `plant-oee.json`
**Files:**
- Modify: `content/curated-articles/plant-oee.json`
- [ ] **Step 1:** Rewrite `plant-oee.json` to the high-depth standard.
- [ ] **Step 2:** Validate JSON structure and section count.

### Task 8: Update `provider-utilization.json`
**Files:**
- Modify: `content/curated-articles/provider-utilization.json`
- [ ] **Step 1:** Rewrite `provider-utilization.json` to the high-depth standard.
- [ ] **Step 2:** Validate JSON structure and section count.

### Task 9: Update `revenue-cycle-denial-rate.json`
**Files:**
- Modify: `content/curated-articles/revenue-cycle-denial-rate.json`
- [ ] **Step 1:** Rewrite `revenue-cycle-denial-rate.json` to the high-depth standard.
- [ ] **Step 2:** Validate JSON structure and section count.

### Task 10: Update `scrap-rate.json`
**Files:**
- Modify: `content/curated-articles/scrap-rate.json`
- [ ] **Step 1:** Rewrite `scrap-rate.json` to the high-depth standard.
- [ ] **Step 2:** Validate JSON structure and section count.

### Task 11: Update `throughput.json`
**Files:**
- Modify: `content/curated-articles/throughput.json`
- [ ] **Step 1:** Rewrite `throughput.json` to the high-depth standard.
- [ ] **Step 2:** Validate JSON structure and section count.

### Task 12: Update `warehouse-picks-per-hour.json`
**Files:**
- Modify: `content/curated-articles/warehouse-picks-per-hour.json`
- [ ] **Step 1:** Rewrite `warehouse-picks-per-hour.json` to the high-depth standard.
- [ ] **Step 2:** Validate JSON structure and section count.
